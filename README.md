
  # Landing page EveryMed

  This is a code bundle for Landing page EveryMed. The original project is available at https://www.figma.com/design/njMYrndxzoghZ6PXxachU2/Landing-page-EveryMed.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  